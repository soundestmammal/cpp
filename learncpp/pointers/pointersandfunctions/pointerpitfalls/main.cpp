#include <iostream>
#include <vector>

using namespace std;

int main()
{
    // Uninitialized pointers
    int *p1;
    // Dangling Pointers
    // Pointer that is pointing to released memory
    // Pointer that points to memory that is invalid
    // Memory Leak
    // If you allcoate the memory but lose the pointer, then you lose access to that memory.
    return 0;
}