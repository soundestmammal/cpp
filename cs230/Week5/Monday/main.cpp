#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;

int main () {

    // vector<int> v1 = { 10, 20, 30, 40, 50 };
    // for_each(v1.begin(), v1.end(), [](int i) {
    //     cout << i*2 << endl;
    // });

    // auto v2 = for_each(v1.begin(), v1.end(), [](int i) {
    //     return 2*i;
    // });

    // auto greaterThan25 = count_if(v1.begin(), v1.end(), [](int i) {
    //     return (i > 25);
    // });

    // int current;
    // int totalAnswer = count(v1.begin(), v1.end(), [int current](int i) -> int {
    //     [current](int i) {
    //         current += i;
    //     } 
    // });

    // vector<int> v1 = { 10, 20 , 30 , 40 , 50 };

    // int smallNum = 2;
    // auto is_greater_than_25 = [smallNum]
    return 0;
}