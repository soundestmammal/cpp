#include <vector>
#include <algorithm>
#include <cmath>
#include <iostream>
using namespace std;

int main() {
    
    double val = 1;
    int pow2 = [](int x) {
        return pow(x, 2);
    };

    pow2(10);

    return 0;
}