#include <iostream>
#include <string>
#include <math.h>

using namespace std;

int main() {
    // How to determine if a number is even or odd?
    // The % is the mod operator (remainder)
    // The int conversion of a.x is a

    double x {9.5};
    cout << fmod(x,2) << endl;

    return 0;
}